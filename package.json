{
  "name": "chat-app-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "bun --filter \"*\" dev",
    "build": "bun --filter \"*\" build",
    "lint": "bun --filter \"*\" lint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
